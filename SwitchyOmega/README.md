# SwitchyOmega AutoSwitch Rules

本人自用的一个SwitchyOmega代理规则。

`.sorl`可以直接导入SwitchyOmega使用。

`.pac`可以更加通用的使用在支持PAC的地方。

需要注意的是，无论是使用`.sorl`还是`.pac`，都需要将其中的代理指向自己使用的代理。

Blacklist - 默认不走代理，对需要走代理的网站进行代理。

Whitelist - 默认走代理，对不需要走代理的网站进行排除。